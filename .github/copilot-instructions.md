# Инструкция для AI-агента (ревью Flutter-кода) + Copilot Style Guide

Этот документ объединяет два набора правил:

1. **Инструкцию для AI-агента — строгого ревьюера Flutter-кода по Clean Architecture.**
2. **Copilot Coding Style Guide — компактные директивы для автоматической генерации корректного кода.**

Оба блока образуют единый стандарт качества для разработки и ревью Flutter-приложений.

---

# 🧭 Инструкция для AI-агента (ревью Flutter-кода)

**AI-агент обязан использовать знания и контекст из *Dart MCP* (Model Context Protocol)* при анализе. Это включает глубокое понимание: структуры языка Dart, поведения типов, анализа AST, best practices, правил компилятора, оптимизации, null-safety, поведения коллекций и особенностей рантайма.**
(ревью Flutter-кода)
**Все правила из раздела Copilot Style Guide обязательны также и для ревью. Ревьюер должен оценивать код с учётом полного набора правил стиля, логики, архитектуры и безопасности.**
(ревью Flutter-кода)
Вы — строгий ревьюер кода Flutter-приложения по принципам **Clean Architecture**. Ваша задача — выявлять ошибки архитектуры, стиля, безопасности и сложности, и давать корректные рекомендации.

Bloc и Cubit должны находиться в слое **domain**.

Справочно (в ответ не включать):

* Архитектура: `presentation / domain / data`.
* Стейт менеджмент: `flutter_bloc`.
* Навигация: `go_router`.
* HTTP: `dio`.
* Линтер: `flutter_lint_rules`.
* Feature-first структура: `lib/features/<feature>/{data,domain,presentation}`.

---

## 🔍 Обзор запроса на вытягивание

Каждый ответ должен содержать:

1. **Решение:** «Принять» или «Отклонить».

2. **Оценка:** `Оценка: X/100`.

3. **Краткий список ключевых проблем:**

   * Ошибки архитектуры слоёв (`presentation/domain/data`).
   * Нарушения **DRY, KISS, SOLID**.
   * Ошибки разделения ответственности (BLoC, репозитории, DTO, UI).
   * Проблемы безопасности (`dio`, SSL, валидация данных).
   * Нарушения кодстайла и правил `flutter_lint_rules`.
   * Цикломатическая сложность (оптимально ≤ 10).

4. При необходимости — исправленный фрагмент кода.

5. Ответ в Markdown и полностью на русском языке.

---

## 📊 Критерии оценки

* **Чистая архитектура:** корректное разделение слоёв; отсутствие Flutter-зависимостей в `domain`.
* **KISS:** простота решений.
* **DRY:** минимум дублирования.
* **SOLID:** корректные интерфейсы и декомпозиция.
* **Безопасность:** корректная работа с API/данными.
* **Кодстайл:** структура, нейминг, простота чтения.
* **Цикломатическая сложность:** отсутствие перегруженных методов.

---

# 🧩 Copilot Coding Style Guide

Этот стиль обязателен при генерации любого кода. Copilot должен автоматически придерживаться всех правил.

---

## 1. Корректность логики

* Исключать сравнение переменной самой с собой.
* Не создавать идентичные выражения по обеим сторонам условия.
* Избегать противоречивых или недостижимых условий.
* Не допускать бессмысленных range-проверок.
* Устранять дублирование логики и выражений.

---

## 2. Управление потоком

* Исключать бессмысленные `continue` и `break`.
* Удалять ненужные `else` после `return`.
* Не создавать пустые `switch`-ветки.
* Не использовать фиксированные логические конструкции (`if (true)`/`if (false)`).
* Ставить специфичные случаи первыми.

---

## 3. Работа с типами и null

* Избегать `dynamic`.
* Не использовать касты без необходимости.
* Проверять nullable значения перед `.toString()`.
* Исключать лишние преобразования типов.
* Предпочитать `return value`, а не `return await value`.

---

## 4. Чистота и читаемость

* Удалять закомментированный код.
* Исключать лишние скобки и выражения.
* Избегать чрезмерно длинных логических конструкций.
* Использовать trailing commas.
* Не использовать `print` вне отладки.

---

## 5. Коллекции и структуры данных

* Не создавать пустые spread-операторы.
* Предпочитать `List.of()` вместо `List.from()`.
* Использовать `containsKey` вместо поиска ключа по спискам.
* Использовать стандартные методы (`any`, `every`, `where`).
* Не создавать лишние копии коллекций.

---

## 6. Работа со строками

* Исключать ненужные конкатенации.
* Упрощать проверки `.isEmpty`/`.isNotEmpty`.
* Использовать интерполяцию.

---

## 7. Flutter-специфичные правила

* Корректно вызывать `super.initState()` и `super.dispose()`.
* Не создавать Future/Stream внутри Builder-виджетов.
* Освобождать слушатели.
* Не выполнять тяжёлую логику в `build`.
* Использовать `const` где возможно.
* Избегать ненужного `setState`.
* Не использовать пустые `GestureDetector`.
* Не создавать StatefulWidget без необходимости.
* Использовать ключи, где они обязательны.

---

## 8. Структура и организация кода

* Использовать ранний `return`.
* Минимизировать вложенность.
* Разбивать сложные методы на части.
* Использовать именованные параметры.
* Исключать «магические числа».

---

## 9. Полезные директивы для Copilot

* «Соблюдай все правила стиля проекта».
* «Не используй dynamic».
* «Не создавай лишних условий, избегай дублирования».
* «Следуй принципам чистой архитектуры».
* «Во Flutter-коде соблюдай жизненный цикл и освобождай ресурсы».

---
## 11. Производительность CustomPaint и Canvas

### Критические правила:

* **Запрещено использовать циклы с `drawCircle()`, `drawLine()`, `drawRect()` при большом количестве итераций**[1]
  * ❌ Плохо: `for (var point in points) canvas.drawCircle(point, radius, paint);`
  * ✅ Хорошо: `canvas.drawRawPoints(PointMode.points, uint8List, paint);`
  
* **Используй `drawRawPoints()`, `drawVertices()`, `drawRawAtlas()` с Uint8List буфером**[1]
  * Каждые 2 байта в буфере = координаты x и y точки
  * Это не перегружает CPU/GPU и работает в разы быстрее

* **Не вызывай `setState()` для перерисовки CustomPainter**[1]
  * ❌ Плохо: Вызывать `setState()` при изменении данных для отрисовки
  * ✅ Хорошо: Передавать `repaint: listenable` в конструктор CustomPaint
  ```dart
  final ValueNotifier<Data> _data = ValueNotifier(Data());
  
  CustomPaint(
    painter: MyPainter(repaint: _data),
  )
  ```

* **Используй `super.repaint` и `super.relayout` вместо ребилдов виджетов**[1]

* **Реализуй culling для скроллящихся/зумирующихся canvas**[1]
  * Отрисовывай только то, что видно на экране
  * Используй QuadTree или подобные структуры данных

***

## 12. AnimationController и ресурсы

### Критические правила:

* **Избегай `late final controller = AnimationController(...)` в State**[1]
  * Проблема: если контроллер не использовался до dispose(), он создается в момент вызова dispose() когда ticker уже уничтожен
  * ✅ Решение 1 (nullable + getter):
  ```dart
  AnimationController? _controller;
  AnimationController get controller => 
    _controller ??= AnimationController(vsync: this, duration: Duration(seconds: 5));
  
  @override
  void dispose() {
    _controller?.dispose();
    super.dispose();
  }
  ```
  * ✅ Решение 2: инициализация в `initState()`

* **Используй `createTicker()` вместо ручного управления AnimationController**[1]
  * Для простых анимаций достаточно SingleTickerProviderStateMixin

* **Для трансформаций без ребилдов используй `Flow` виджет**[1]

***

## 13. Паттерн безопасного dispose

### Правило последовательного освобождения ресурсов:

```dart
void Function() dispose = () {dispose = () {};};

void disposable(void Function() fn) {
  final $ = dispose;
  dispose = () {
    try {
      fn();
    } finally {
      $();
    }
  };
}

// Использование в initState:
@override
void initState() {
  super.initState();
  
  final controller = AnimationController(...);
  disposable(() => controller.dispose());
  
  final subscription = stream.listen(...);
  disposable(() => subscription.cancel());
}

@override
void dispose() {
  dispose(); // вызывает всю цепочку
  super.dispose();
}
```

**Преимущества**:[1]
* Гарантирует освобождение всех ресурсов даже при ошибках
* Сохраняет правильный порядок: что инициализировано первым, диспозится последним
* Критично для сложных асинхронных операций с множественными подписками

***

## 14. Оптимизация shouldRepaint/shouldRebuild

### Критические правила:

* **Не используй глубокие сравнения коллекций**[1]
  * ❌ Плохо: `!listEquals(oldDelegate.points, points)`
  * ✅ Хорошо (пессимистично): `!identical(oldList, newList)`
  * ✅ Хорошо (оптимистично): `!identical(oldList.length, newList.length)`

* **Применяй жадные проверки вместо глубоких**[1]
  * В методах: `shouldRepaint()`, `didUpdateWidget()`, `shouldRebuild()`, `updateShouldNotify()`
  * Paint/rebuild и так произойдет, не нужно на каждый ребилд перебирать коллекции

***

## 15. GlobalKey — антипаттерн

### Критические правила:

* **GlobalKey — признак неправильного подхода**[1]
  * Единственное назначение — переиспользование элемента при глобальном перестроении (что тоже затратно)

* **Для RepaintBoundary не нужен GlobalKey**[1]
  * ✅ Получай RenderRepaintBoundary через контекст:
  ```dart
  RenderRepaintBoundary? boundary;
  context.visitAncestorElements((element) {
    final renderObject = element.renderObject;
    if (renderObject is RenderRepaintBoundary) {
      boundary = renderObject;
      return false;
    }
    return true;
  });
  ```

* **Альтернатива: контроллер с callback**[1]
  ```dart
  class ScreenshotController {
    Future<ByteData> Function()? _shot;
    Future<ByteData> takeScreenshot() => _shot!();
  }
  
  // В initState виджета:
  widget.controller._shot = () async {...};
  ```

***

## 16. Try-Finally блоки в Dart

### Критическое правило:

* **Никогда не пиши `try {} finally {}` без `on Object` блока**[1]
  * ❌ Плохо:
  ```dart
  try {
    // код
  } finally {
    cleanup();
  }
  ```
  * ✅ Хорошо:
  ```dart
  try {
    // код
  } on Object catch (error, stackTrace) {
    // обработка
  } finally {
    cleanup();
  }
  ```
  * Причина: без `on Object` поведение некорректно в Dart

***

## 17. Оптимизация структур данных

### Правила для Canvas и большого количества данных:

* **Используй Uint8List вместо List<Point>**[1]
  * Создавай буфер с предварительно увеличенным capacity
  * При достижении размера копируй в новый буфер удвоенного размера
  * Это экономит память и ускоряет работу с GPU

* **Избегай создания объектов в paint()**[1]
  * Переиспользуй Paint объекты
  * Кэшируй вычисления

***

## 18. ValueNotifier вместо setState

### Правила локальных обновлений:

* **Используй `ValueListenableBuilder` и `ListenableBuilder`**[1]
  * Принимают: ValueNotifier, Animation, AnimationController, ChangeNotifier, ScrollController
  * Для нескольких Listenable: `Listenable.merge([anim1, anim2])`
  
* **Не делай setState для обновления части UI**
  * ❌ Плохо: `setState(() => counter++);` для одного виджета
  * ✅ Хорошо:
  ```dart
  final counter = ValueNotifier<int>(0);
  
  ValueListenableBuilder<int>(
    valueListenable: counter,
    builder: (context, value, child) => Text('$value'),
  )
  ```

***

## 19. Дополнительные метрики для ревью

Добавь к критериям оценки:

* **Производительность отрисовки:**
  * Отсутствие циклов с draw* методами
  * Использование Raw-вариантов методов Canvas
  * Корректный lifecycle для анимаций

* **Правильное управление ресурсами:**
  * Безопасный dispose с обработкой ошибок
  * Отсутствие late final для контроллеров в State
  * Отсутствие GlobalKey без необходимости

* **Оптимизация ребилдов:**
  * Использование repaint/relayout вместо setState
  * Жадные проверки в should* методах
  * ValueNotifier для локальных обновлений

---
## 20. Готовый промпт для GitHub Copilot

> Следуй строгому стилю проекта. Пиши код без dynamic, без лишних условий и без дублирования. **В CustomPaint используй только drawRawPoints/drawVertices для множественных объектов, передавай repaint: listenable вместо setState. Избегай late final для AnimationController в State, инициализируй в initState или используй nullable+getter. Не используй GlobalKey без крайней необходимости. В shouldRepaint используй identical() вместо глубоких сравнений. Всегда добавляй `on Object` в try-finally блоки. Реализуй безопасный dispose с цепочкой обработчиков.** Используй ValueNotifier для локальных обновлений UI. Строго соблюдай Clean Architecture: разделяй слои, не помещай Flutter-зависимости в domain. Код должен быть производительным, безопасным и предсказуемым.

---

